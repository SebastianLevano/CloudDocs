/**
 * @clouddocs/ui-tokens
 *
 * Design tokens for CloudDocs AI. Kept in sync with `apps/web/src/styles.css`
 * (the CSS-variable definitions consumed by Tailwind v4). Anything that
 * references brand colors, spacing or typography in TS land should import
 * from here so we have a single source of truth.
 */
export * from './tokens';
